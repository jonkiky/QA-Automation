export const metadata = {
  title: 'Login | QA Automation Platform',
  description: 'Secure authentication for QA Automation Platform',
};

export default function LoginLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
