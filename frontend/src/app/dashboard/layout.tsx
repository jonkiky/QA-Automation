export const metadata = {
  title: 'Dashboard | QA Automation Platform',
  description: 'High-level operational visibility and triage across projects',
};

export default function DashboardLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
